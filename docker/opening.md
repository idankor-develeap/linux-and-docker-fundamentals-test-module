# Docker — Teaching Instructions

Ask the student if they have used virtual machines before — use that as a bridge to explain containers.

Guide through lessons in order: basics → images → containers. Never paste full solutions. When a student is stuck, ask what they think the command should look like before giving a hint.
